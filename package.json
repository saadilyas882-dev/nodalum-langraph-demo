{
  "name": "nodalum-langgraph-demo",
  "version": "1.0.0",
  "description": "LangGraph legal AI pipeline with deterministic verifier — Nodalum demo",
  "main": "dist/run.js",
  "scripts": {
    "start": "ts-node src/run.ts",
    "build": "tsc",
    "test": "jest --runInBand --forceExit"
  },
  "dependencies": {
    "@langchain/anthropic": "^0.3.14",
    "@langchain/core": "^0.3.26",
    "@langchain/langgraph": "^0.2.36",
    "dotenv": "^16.3.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.17.9",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.ts"],
    "testTimeout": 30000
  }
}
